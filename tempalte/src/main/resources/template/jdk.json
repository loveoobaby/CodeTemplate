[{"template":"        ScheduledExecutorService executor = Executors.newScheduledThreadPool(2);\n        ScheduledFuture future = executor.schedule(new Runnable() {\n            @Override\n            public void run() {\n                System.out.println(\"60 second later\");\n            }\n        }, 60, TimeUnit.SECONDS);\n        future.get();\n        executor.shutdown();","name":"ScheduleExecutorService"}]